package chap05.lecture.reftype.enumType;

public enum MyEnum {
	
	// Enumeration 의 약자
	COFFEE,
	WATER,
	JUICE
	// ,로 구분해서 여러 개의 값을 나열
	// 나열된 값은 상수처럼 사용되기 때문에 enum에 정의된 값도 대문자 snakecase로 작성
	// enum 타입의 값은 저 정해진 세개 중 하나만 가능 
	
}
